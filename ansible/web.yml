---

- name: Provision Stack 
  hosts: localhost
  connection: local
  gather_facts: False
  vars_files:
    - "./vars/web/bootstrap.yaml"
    - "./vars/web/cloudformation.yaml"
  pre_tasks:
    - include_vars: "./var/{{ item }}.yaml"
      with_items: include
  roles:
    - "{{ stack }}"
